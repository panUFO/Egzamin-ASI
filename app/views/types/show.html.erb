<div style="padding-left:50px; padding-right:50px">



<div style="padding-left:100px; padding-right:100px">
<div class="jumbotron">

<h2>Bohaterowie danego typu: <%= @type.name %> <%= @type.desc %></h2>
<table class="table table-responsive table-hover">
  <thead>
    <tr>
      <th>Nazwa</th>
      <th>Cena</th>
      <% if user_signed_in? %>
      <th colspan="3"></th>
      <% else  %>
      <th colspan="2"></th>
  <% end %>
    </tr>
  </thead>
  
  <tbody>
    
    <%= render @type.champs %>
  
  </tbody>
</table>

<% if user_signed_in? %>

<%= render 'champs/form' %>
<% else  %>
  <% end %>
<div style="padding-left:50px;padding-top:20px; ">
<%= link_to 'Edytuj', edit_type_path(@type) %> |
<%= link_to 'Wróć', types_path %>
</div>
</div>

</div>
</div>
